# Not used yet — Week 1 ships with threat-intel calls stubbed out
# (see app/threat_intel/stub.py). These are placeholders for Phase 2,
# when real clients read their keys from here instead of the stub.

# GOOGLE_SAFE_BROWSING_API_KEY=
# VIRUSTOTAL_API_KEY=
# PHISHTANK_API_KEY=
